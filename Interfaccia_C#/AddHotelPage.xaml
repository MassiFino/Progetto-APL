<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Interfaccia_C_.ViewModel"
             x:Class="Interfaccia_C_.AddHotelPage"
             x:DataType="vm:AddHotelPageViewModel"
             Title="Add Hotel Page"
             BackgroundColor="White"
             Padding="0">


        <ScrollView>
        <VerticalStackLayout Margin="10" Spacing="15">

                <!-- Contenuto scrollabile -->
                <StackLayout>
                <Label 
                    Text="Welcome to the Add Hotel Page!" 
                    FontSize="18" 
                    FontAttributes="Italic" 
                    Margin="0,10,0,5" 
                TextColor="Green" />
            </StackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
